{
  "name": "genome8",
  "version": "1.0.0",
  "description": "Context engine for AI agents — nodes + journeys, compile connections, smart excerpts",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "genome": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  }
}
